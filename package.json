{
  "name": "smart-lamp",
  "version": "1.0.0",
  "description": "Smart Alarm & Lighting Control App",
  "private": true,
  "workspaces": [
    "mobile",
    "backend"
  ],
  "scripts": {
    "mobile": "cd mobile && npm start",
    "backend": "cd backend && npm run dev",
    "install:all": "npm install && cd mobile && npm install && cd ../backend && npm install"
  }
}
